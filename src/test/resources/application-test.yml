spring:
  application:
    name: comments-service
  datasource:
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create

feign:
  auth-service:
    value: authService
    url: http://localhost:9998/api/auth